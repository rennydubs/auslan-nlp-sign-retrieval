# ============================================================
# Auslan Sign Retrieval System — Environment Variables
# Copy this file to .env and fill in your values.
# .env is gitignored and never committed.
# ============================================================

# ---- Backend ------------------------------------------------

# Flask/FastAPI secret key (change in production!)
SECRET_KEY=auslan-sign-system-dev

# Ollama LLM host (default: local; override for remote instance)
OLLAMA_HOST=http://localhost:11434

# LLM model name (qwen3:8b recommended; llama3.2:3b for lighter setups)
LLM_MODEL=qwen3:8b

# Set to false to disable LLM fallback entirely
# LLM_ENABLED=true

# Flask debug mode (set false in production)
FLASK_DEBUG=false

# ---- Frontend -----------------------------------------------

# Backend API URL (used by Next.js at build time + runtime)
NEXT_PUBLIC_API_URL=http://localhost:8000

# ---- Docker Compose -----------------------------------------

# These are automatically used by docker-compose.yml.
# When running with docker-compose, OLLAMA_HOST should be:
#   OLLAMA_HOST=http://ollama:11434   (container DNS)
# When running locally (no Docker), use:
#   OLLAMA_HOST=http://localhost:11434
